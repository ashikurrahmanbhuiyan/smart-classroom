<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/resource_sharing.css">
</head>
<body>
    <div class="navbar">
        <div class="nav-item" data-index="0">
            <a href="/attendance_sheet/all">Attendance Sheet</a>
        </div>
        <div class="nav-item" data-index="1"><h4>Course Tracker</h4></div>
        <div class="nav-item" data-index="2"><h4>Assignment</h4></div>
        <div class="nav-item" data-index="3"><h4>resourse Sharing</h4></div>
        <div class="slider"></div>
    </div>

    <script>
        const navItems = document.querySelectorAll('.nav-item');
        const slider = document.querySelector('.slider');

        navItems.forEach((item, index) => {
            item.addEventListener('click', () => {
                slider.style.left = `${index * 25}%`;

                navItems.forEach(i => i.classList.remove('active'));
                item.classList.add('active');
            });
        });
    </script>
</body>
</html> -->


<h1>Welcome to Home</h1>
<p>This is the home page.</p>