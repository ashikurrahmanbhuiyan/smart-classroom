mongoose
//     .connect('mongodb://localhost:27017/login-system', {
//         useNewUrlParser: true,
//         useUnifiedTopology: true,
//     })
//     .then(() => console.log('MongoDB Connected'))
//     .catch((err) => console.log(err));

{/* <div class="course-item">
                        
                        <% courses.forEach((element) =>{ %>
                            <h4><%= element.course_name %></h4>
                            <span><%= element.teacher_email %></span>
                        </div>
                        <button>Open</button>
                       <% }) %>
                    </div> */}

                     // const coursesByTeacher = findCourses.departments.flatMap(department =>
    //     department.courses.filter(course => course.teacher_email === req.user.email)
    // );
    // const batches = findCourses.departments
    // .filter(department =>
    //   department.courses.some(course => course.teacher_email === req.user.email)
    // )
    // .map(department => department.batch_name);